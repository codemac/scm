(define-module (codemac simulation weight))

(define *usa-base-units*
  '(feet
    fluid-ounce
    pound
    fahrenheit
    ))

(define *si-base-units*
  '(meter
    kelvin
    mole
    ampere
    candela
    kilogram
    second))

(define (ratio-conversion from ratio to-unit))

(define (make-unit))
